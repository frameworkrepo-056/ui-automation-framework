# PROD ? read-only smoke tests ONLY, never regression
# Jenkins Validate stage blocks tagless prod runs
base.url=https://www.myapp.com
app.username=prod_readonly
app.password=Prod@ReadOnly1
headless=true
explicit.wait=20
retry.count=1